{
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "enum": ["morpheus"]
      },
      "job": {
        "type": "string",
        "enum": ["zion resident"]
      },
      "updatedAt": {
        "type": "string"
      }
    },
    "required": [
      "name",
      "job",
      "updatedAt"
    ]
  }